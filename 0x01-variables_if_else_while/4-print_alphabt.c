#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	char lowc, e, q;

	e = 'e';
	q = 'q';

	while (lowc = 'a'; lowc <= 'z'; low++)
	{
		if (lowc != e && lowc != q)
			putchar(low);
	}
	putchar('\n');

	return (0);
}
