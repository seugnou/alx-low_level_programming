#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("%d\n", mull(98, 1024));
	printf("%d\n", mull(-402, 4096));

	return (0);
}
